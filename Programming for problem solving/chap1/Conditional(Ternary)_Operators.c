#include <stdio.h>
int main() {
  int a = 10, b = 5, c =;
  int *ptr = &a;

// Conditional(Ternary)Operators
printf("Conditional Operators:\n");
int max = (a > b) ? a : b;
printf("Max of a and b: %d\n\n", max);

return 0;
}
